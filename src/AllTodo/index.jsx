import React from "react";

const AllTodo = (props) => {
    return (
        <section className="todos-section outmost-container">
            <div>Your tasks!</div>
        </section>
    )
};

export default AllTodo;
